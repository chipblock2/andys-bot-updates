let C=window.ASK_SHROPSHIRE_CONTENT;const app=document.getElementById('app');const now=new Date();const saved=new Set(JSON.parse(localStorage.getItem('askshrop_saved')||'[]'));const fmt=d=>new Intl.DateTimeFormat('en-GB',{weekday:'short',day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'}).format(new Date(d));const futureEvents=()=>C.events.filter(e=>e.status==='Published' && (!e.end || new Date(e.end)>=now));function render(){app.innerHTML=`<header class="top"><div class="topin"><div class="logo">A</div><div class="brand"><b>Ask Shropshire</b><small>VERIFIED LOCAL GUIDE</small></div><div class="status">Content Hub synced ${fmt(C.syncedAt)}</div></div></header><main class="shell"><section id="home" class="view active"><div class="hero"><div class="heroCopy"><small>SHROPSHIRE • LIVE CONTENT PILOT</small><h1>What do you want to do?</h1><p>Ask once and get a practical local answer using current events and verified places from the Ask Shropshire Content Hub.</p><form class="ask" id="askForm"><input id="askInput" placeholder="What’s on near Telford this weekend?"><button>Ask Shropshire</button></form><div class="chips"><button class="chip" data-q="this weekend">This weekend</button><button class="chip" data-q="family">Family</button><button class="chip" data-q="theatre">Theatre</button><button class="chip" data-q="walk">Walks</button><button class="chip" data-q="bridgnorth">Bridgnorth</button></div></div></div>${eventsSection('What’s on now',futureEvents().slice(0,6))}${placesSection('Featured places',C.places)}</section><section id="explore" class="view"><div class="section"><div class="sectionHead"><div><h2>Explore Shropshire</h2><p>Live Content Hub places and businesses.</p></div><span class="sync">Source-led</span></div><div class="tabs"><button class="tab active" data-type="All">All</button><button class="tab" data-type="Attraction">Things to do</button><button class="tab" data-type="Walk Outdoor">Walks</button><button class="tab" data-type="Venue">Venues</button></div><div id="placeGrid" class="grid">${placeCards(C.places)}</div></div></section><section id="ask" class="view"><div class="section"><div class="sectionHead"><div><h2>Ask Shropshire</h2><p>Search events and verified places from the current content snapshot.</p></div></div><form class="ask" id="askPageForm"><input id="askPageInput" placeholder="Try: family events, Ironbridge, walks..."><button>Search</button></form><div id="answer" class="section"></div></div></section><section id="plan" class="view"><div class="section planner"><h2>Plan a day</h2><p>Build a practical starter itinerary from verified places. Live journey, weather and opening-hour constraints are the next connector stage.</p><form class="plannerForm" id="plannerForm"><select id="area"><option>Telford</option><option>Ironbridge</option><option>Shrewsbury</option><option>Church Stretton</option><option>Bridgnorth</option></select><select id="group"><option>Adults</option><option>Family</option><option>Couple</option><option>Accessible day out</option></select><select id="priority"><option>Mix of everything</option><option>Heritage</option><option>Outdoors</option><option>Events</option></select><button class="primary">Build plan</button></form><div class="planResult" id="planResult"></div></div></section><section id="saved" class="view"><div class="section"><div class="sectionHead"><div><h2>Saved</h2><p>Your saved events and places stay on this device.</p></div></div><div id="savedGrid" class="grid"></div></div></section></main><nav class="bottom"><div class="bottomin"><button class="nav active" data-view="home"><b>⌂</b>Home</button><button class="nav" data-view="explore"><b>⌖</b>Explore</button><button class="nav askNav" data-view="ask"><b>✦</b>Ask</button><button class="nav" data-view="plan"><b>☷</b>Plan</button><button class="nav" data-view="saved"><b>♡</b>Saved</button></div></nav><div class="detail" id="detail"><div class="sheet" id="sheet"></div></div>`;wire()}function eventsSection(title,items){return `<div class="section"><div class="sectionHead"><div><h2>${title}</h2><p>${items.length} current listings from approved/primary sources.</p></div><span class="sync">Past events auto-hidden</span></div><div class="grid">${eventCards(items)}</div></div>`}function eventCards(items){if(!items.length)return `<div class="empty">No current events match yet.</div>`;return items.map(e=>`<article class="card" data-kind="event" data-id="${e.id}"><div class="eventTop"><span class="cat">${e.category}</span></div><div class="cardBody"><h3>${e.name}</h3><div class="meta">${e.town}${e.venue?' · '+e.venue:''}<br>${fmt(e.start)}</div><p class="desc">${e.description||''}</p><div class="row"><button class="btn dark details">Details</button><button class="btn save">${saved.has(e.id)?'♥ Saved':'♡ Save'}</button></div></div></article>`).join('')}function placesSection(title,items){return `<div class="section"><div class="sectionHead"><div><h2>${title}</h2><p>Verified starter records from the Content Hub.</p></div></div><div class="grid">${placeCards(items)}</div></div>`}function placeCards(items){return items.map(p=>`<article class="card placeCard" data-kind="place" data-id="${p.id}"><div class="placeIcon">${p.type==='Walk Outdoor'?'🥾':p.type==='Venue'?'🎭':'📍'}</div><h3>${p.name}</h3><div class="meta">${p.town} · ${p.type}</div><p class="desc">${p.description}</p><div class="row"><button class="btn dark details">Details</button><button class="btn save">${saved.has(p.id)?'♥ Saved':'♡ Save'}</button></div></article>`).join('')}function wire(){document.querySelectorAll('.nav').forEach(b=>b.onclick=()=>show(b.dataset.view));document.querySelectorAll('.chip').forEach(b=>b.onclick=()=>search(b.dataset.q));document.getElementById('askForm').onsubmit=e=>{e.preventDefault();search(document.getElementById('askInput').value)};document.getElementById('askPageForm').onsubmit=e=>{e.preventDefault();search(document.getElementById('askPageInput').value)};document.body.addEventListener('click',e=>{const card=e.target.closest('[data-kind]');if(!card)return;if(e.target.classList.contains('save')){toggleSave(card.dataset.id,e.target);e.stopPropagation();return}if(e.target.classList.contains('details')||e.target===card||card.contains(e.target))openDetail(card.dataset.kind,card.dataset.id)});document.getElementById('detail').onclick=e=>{if(e.target.id==='detail'||e.target.classList.contains('close'))document.getElementById('detail').classList.remove('show')};document.querySelectorAll('.tab').forEach(t=>t.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));t.classList.add('active');const type=t.dataset.type;document.getElementById('placeGrid').innerHTML=placeCards(type==='All'?C.places:C.places.filter(p=>p.type===type))});document.getElementById('plannerForm').onsubmit=e=>{e.preventDefault();buildPlan()};renderSaved()}function show(id){document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id===id));document.querySelectorAll('.nav').forEach(n=>n.classList.toggle('active',n.dataset.view===id));if(id==='saved')renderSaved();scrollTo({top:0,behavior:'smooth'})}function search(q){q=(q||'').toLowerCase().trim();show('ask');const ev=futureEvents().filter(e=>[e.name,e.category,e.venue,e.town,e.description].join(' ').toLowerCase().includes(q.split(' ')[0])||q.split(' ').some(w=>w.length>3&&[e.name,e.category,e.venue,e.town,e.description].join(' ').toLowerCase().includes(w)));const pl=C.places.filter(p=>q.split(' ').some(w=>w.length>3&&[p.name,p.type,p.town,p.description].join(' ').toLowerCase().includes(w)));document.getElementById('answer').innerHTML=`<div class="sectionHead"><div><h2>Results</h2><p>${ev.length+pl.length} matches for “${q||'Shropshire'}”.</p></div><span class="sync">Content Hub snapshot</span></div>${ev.length?`<h3>Events</h3><div class="grid">${eventCards(ev)}</div>`:''}${pl.length?`<h3>Places</h3><div class="grid">${placeCards(pl)}</div>`:''}${!ev.length&&!pl.length?'<div class="empty">No verified match in the current snapshot yet. This is exactly the kind of gap the Content Hub will help us identify and fill.</div>':''}`}function findItem(kind,id){return kind==='event'?C.events.find(x=>x.id===id):C.places.find(x=>x.id===id)}function openDetail(kind,id){const x=findItem(kind,id);if(!x)return;document.getElementById('sheet').innerHTML=`<button class="close">×</button><span class="source">${kind==='event'?(x.sourceName||'Verified source'):'Verified place record'}</span><h2>${x.name}</h2><p class="meta">${x.town||''}${x.venue?' · '+x.venue:''}${x.start?'<br>'+fmt(x.start):''}</p><p>${x.description||''}</p>${x.price?`<p><b>From £${x.price.toFixed(2)}</b></p>`:''}<div class="row">${x.bookingUrl?`<a class="btn dark" target="_blank" href="${x.bookingUrl}">Book / official page</a>`:''}${x.website?`<a class="btn dark" target="_blank" href="${x.website}">Official website</a>`:''}${x.sourceUrl?`<a class="btn" target="_blank" href="${x.sourceUrl}">View source</a>`:''}</div>${x.lastChecked?`<p class="meta">Last checked ${fmt(x.lastChecked)}</p>`:''}`;document.getElementById('detail').classList.add('show')}function toggleSave(id,btn){saved.has(id)?saved.delete(id):saved.add(id);localStorage.setItem('askshrop_saved',JSON.stringify([...saved]));btn.textContent=saved.has(id)?'♥ Saved':'♡ Save';renderSaved()}function renderSaved(){const box=document.getElementById('savedGrid');if(!box)return;const ev=C.events.filter(x=>saved.has(x.id));const pl=C.places.filter(x=>saved.has(x.id));box.innerHTML=(ev.length||pl.length)?eventCards(ev)+placeCards(pl):'<div class="empty">Nothing saved yet. Tap ♡ Save on an event or place.</div>'}function buildPlan(){const area=document.getElementById('area').value;const priority=document.getElementById('priority').value;const local=C.places.filter(p=>p.town.toLowerCase().includes(area.toLowerCase())||area==='Telford'&&p.town==='Ironbridge');const nearby=futureEvents().filter(e=>e.town.toLowerCase().includes(area.toLowerCase())||area==='Telford'&&/Telford|Wellington|Newport/.test(e.town));const p=local[0]||C.places[0];const out=document.getElementById('planResult');out.style.display='block';out.innerHTML=`<b>Starter plan for ${area}</b><br><br>10:00 — ${p.name}<br>12:30 — Lunch / local break (live food connector next)<br>${nearby[0]?`14:30 — ${nearby[0].name} (${fmt(nearby[0].start)})`:'14:30 — Explore another verified local place'}<br>16:30 — Flexible finish / weather-aware alternative<br><br><small>Priority: ${priority}. This itinerary is a prototype built from verified Content Hub records; travel and opening-hour validation comes next.</small>`}async function boot(){try{const r=await fetch('https://raw.githubusercontent.com/chipblock2/andys-bot-updates/main/ask-shropshire/content.json',{cache:'no-store'});if(r.ok){const live=await r.json();if(live&&Array.isArray(live.events)&&Array.isArray(live.places))C=live}}catch(e){console.warn('Live feed unavailable; using bundled snapshot',e)}render()}boot();